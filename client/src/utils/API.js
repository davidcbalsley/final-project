import axios from "axios";

export default {
  // Insert get, delete, and save requests, with axios calls to routes, here
  /*
  getBooks: function() {
    return axios.get("/api/books");
  }
  */
};
